{
  "name": "zenbooks-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "json-server db.json --host 0.0.0.0 --port $PORT --static ./"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3",
    "cors": "^2.8.5"
  }
}
